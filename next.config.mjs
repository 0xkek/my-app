// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NOTE: Does NOT include typescript: { ignoreBuildErrors: true }
};
export default nextConfig;